/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AssignVehicleRequest } from '../../models/assign-vehicle-request';
import { BookingResponse } from '../../models/booking-response';

export interface Assign$Params {
  id: string;
  body: AssignVehicleRequest;
}

export function assign(
  http: HttpClient,
  rootUrl: string,
  params: Assign$Params,
  context?: HttpContext,
): Observable<StrictHttpResponse<BookingResponse>> {
  const rb = new RequestBuilder(rootUrl, assign.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(rb.build({ responseType: 'blob', accept: '*/*', context })).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BookingResponse>;
    }),
  );
}

assign.PATH = '/api/admin/bookings/{id}/assign-vehicle';
