/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { VehicleClassResponse } from '../../models/vehicle-class-response';

export interface List4$Params {}

export function list4(
  http: HttpClient,
  rootUrl: string,
  params?: List4$Params,
  context?: HttpContext,
): Observable<StrictHttpResponse<Array<VehicleClassResponse>>> {
  const rb = new RequestBuilder(rootUrl, list4.PATH, 'get');
  if (params) {
  }

  return http.request(rb.build({ responseType: 'blob', accept: '*/*', context })).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<VehicleClassResponse>>;
    }),
  );
}

list4.PATH = '/api/admin/vehicle-classes';
