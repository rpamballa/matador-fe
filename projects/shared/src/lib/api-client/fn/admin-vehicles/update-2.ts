/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UpdateVehicleRequest } from '../../models/update-vehicle-request';
import { VehicleResponse } from '../../models/vehicle-response';

export interface Update2$Params {
  id: string;
  body: UpdateVehicleRequest;
}

export function update2(
  http: HttpClient,
  rootUrl: string,
  params: Update2$Params,
  context?: HttpContext,
): Observable<StrictHttpResponse<VehicleResponse>> {
  const rb = new RequestBuilder(rootUrl, update2.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(rb.build({ responseType: 'blob', accept: '*/*', context })).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<VehicleResponse>;
    }),
  );
}

update2.PATH = '/api/admin/vehicles/{id}';
